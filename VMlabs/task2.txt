cd ~/DevOps-Intro
git checkout -B feature/lab6
mkdir -p labs

# 1) Выполни команды задания
sudo docker ps -a
sudo docker pull ubuntu:latest
sudo docker images ubuntu
sudo docker run -it --name ubuntu_container ubuntu:latest
# внутри контейнера:
cat /etc/os-release
ps aux
exit

sudo docker save -o ubuntu_image.tar ubuntu:latest
ls -lh ubuntu_image.tar
sudo docker rmi ubuntu:latest
sudo docker rm ubuntu_container
sudo docker rmi ubuntu:latest

# 2) Доп. данные для отчета
sudo docker images
sudo docker inspect ubuntu:latest --format '{{len .RootFS.Layers}}' || true

# 3) Обновить файл отчета и запушить
nano labs/submission6.md
git add labs/submission6.md
git commit -m "Update lab6 task1"
git push -u origin feature/lab6
